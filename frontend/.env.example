# Base URL of the FastAPI backend.
# Leave blank in dev — Vite proxies /api (including live voice WebSocket) to http://127.0.0.1:8003.
# Set explicitly for packaged Electron builds, e.g. http://127.0.0.1:8003
VITE_API_BASE_URL=
